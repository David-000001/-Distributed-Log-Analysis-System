# ── Local CLI dependencies (python main.py / agent / server / analyse) ──────
# These are used only by the original Python CLI scripts.
# Netlify Functions (netlify/functions/*.py) use Python stdlib only
# and do NOT require any third-party packages.
colorama>=0.4.6
tabulate>=0.9.0
